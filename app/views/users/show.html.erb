<p>
  <strong>Welcome, <%= @user.username %> </strong>
</p>

  <p><%= link_to 'Create New Entry', new_recipe_path(@recipe) %></p>

<h2>Recipes</h2>

  <% @recipes.each do |recipe| %>
    <ol>
      <li><%= link_to recipe.name, recipe_path(recipe) %></li>
    </ol>
  <% end %>

  <% if current_user && current_user.admin = true %>
  <% end %>
